sequenceDiagram
    autonumber
    participant Server
    participant World
    participant GameMode as GameMode (Server)
    participant GameState as GameState (S/C)
    participant NetDriver as NetDriver (Listen)
    participant Client
    participant PC_S as PlayerController (Server)
    participant Pawn_S as Pawn (Server)
    participant PC_C as PlayerController (Client Owner)
    participant Pawn_C as Pawn (Client Owner)
    participant HUD as HUD (Client Owner)
    participant PCM as PlayerCameraManager (Client Owner)

    %% ---------- 서버 프로세스/맵 로드 ----------
    Server->>World: LoadMap()
    World->>World: BeginPlay() ▶️ (월드 시작)
    World->>GameMode: Spawn & Init
    GameMode-->>GameMode: BeginPlay() ▶️ (서버 전용)
    World->>GameState: Spawn & Init
    GameState-->>GameState: BeginPlay() ▶️ (서버측 GameState)
    GameMode->>NetDriver: InitListen() (소켓 오픈)

    %% ---------- 클라이언트 접속 ----------
    Client->>NetDriver: Connect(ServerIP)
    NetDriver->>GameMode: Login()
    GameMode->>Server: Spawn PlayerController (Server)
    PC_S-->>PC_S: BeginPlay() ▶️ (월드가 이미 시작 상태)
    GameMode->>Server: SpawnDefaultPawnFor(PC_S)
    Pawn_S-->>Pawn_S: BeginPlay() ▶️
    PC_S->>Pawn_S: Possess()
    Pawn_S-->>Pawn_S: PossessedBy(PC_S) (서버 권한 확립)

    %% ---------- 복제 & 클라이언트 로컬 생성 ----------
    Server-->>Client: Replicate GameState/PC/Pawn etc.
    Client->>GameState: Spawn (Client copy)
    GameState-->>GameState: BeginPlay() ▶️ (클라측 GameState)
    Client->>PC_C: Spawn (Owner PC)
    PC_C-->>PC_C: BeginPlay() ▶️
    Client->>Pawn_C: Spawn (Owner Pawn)
    Pawn_C-->>Pawn_C: BeginPlay() ▶️

    %% ---------- 소유자 로컬 초기화 경로 ----------
    PC_C->>PC_C: OnRep_Pawn()
    PC_C->>Pawn_C: SetPawn()
    PC_C->>PC_C: AcknowledgePossession(Pawn_C) (입력/카메라/HUD 로컬 초기화)
    PC_C->>HUD: Spawn
    HUD-->>HUD: BeginPlay() ▶️
    PC_C->>PCM: Spawn
    PCM-->>PCM: BeginPlay() ▶️

chagpt5 발췌 (정보 재확인)