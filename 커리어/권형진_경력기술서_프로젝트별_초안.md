# 권형진 경력기술서 초안

> 작성 기준: `git_history_user_activity_implementation.md`, `GitHistory_KwunHyeongJin.md`, `퇴사자 인수인계 문서_권형진.md`, `권형진_개발자 인수인계 문서.md` 및 프로젝트별 기능 문서

## 1. 경력 요약

Unreal Engine 기반 멀티플레이 게임 프로젝트에서 C++ gameplay system, UI/HUD, 네트워크 동기화, 저장/로드, 콘텐츠 연동, QA 안정화, 릴리즈 브랜치 통합 업무를 수행했다.  
주요 업무는 `BackroomCompany`의 V206~V208 기능 개발 및 안정화, `BackroomCleaners`의 렌더링 최적화와 전투/체력바/오디오/HUD 시스템 구현이다.

확인된 Git 이력 기준으로 `BackroomCompany`에서는 2025년 9월 22일부터 2026년 5월 18일까지 총 1,039건의 author 매칭 이력이 있으며, 이 중 일반 커밋 566건, merge commit 473건이 확인된다. `BackroomCleaners`에서는 2025년 11월 21일부터 2026년 4월 9일까지 총 93건의 작업 이력이 확인되며, 실 변경 커밋 64건, merge commit 29건이 확인된다.

### 핵심 역량

- Unreal Engine C++ 기반 gameplay system 설계 및 구현
- 멀티플레이 환경의 RPC, Replication, 서버 권한 상태 관리, client-side UI 표시 분리
- GameplayTag, DataTable, DataAsset 기반 데이터 주도형 시스템 구현
- SaveGame, 진행도, 인벤토리, 허브 보관함, 테크트리 상태 저장/복원
- UMG Widget, WidgetComponent, HUD, Modal, Minimap, Entity Health Bar 등 UI/HUD 시스템 구현
- World Partition, Blueprint, Niagara, Material, Sound, DataAsset 등 Unreal 콘텐츠와 C++ 연동
- QA 이슈, 크래시, null access, 패키징/빌드 오류, 멀티플레이 동기화 문제 대응
- 기능 브랜치 통합, 릴리즈 브랜치 병합, 버전 변경 및 회귀 테스트 지원

### 주요 기술 스택

- Engine: Unreal Engine 5
- Language: C++, Blueprint
- Gameplay: GameplayTag, DataTable, DataAsset, SaveGame, GameState, GameInstanceSubsystem, ActorComponent
- Network: RPC, Replication, NetMulticast, listen server/client 검증
- UI: UMG, WidgetComponent, Screen Space Widget, HUD, Modal, Debug Widget
- Content: World Partition External Actors, Niagara, Material Instance, Sound Cue, Render Target
- Tools: Git, GitHub/GitLab 계열 원격 저장소, Unreal Editor, Rider/Visual Studio 계열 C++ IDE

---

# 프로젝트 1. BackroomCompany

## 프로젝트 개요

- 프로젝트: BackroomCompany
- 장르/성격: Unreal Engine 기반 멀티플레이 협동 게임
- 업무 기간: 2025.09 ~ 2026.05  
  - Git 이력 확인 범위: 2025.09.22 ~ 2026.05.18
- 담당 역할: Unreal Engine C++ 클라이언트/게임플레이 개발, UI/HUD 및 시스템 구현, QA 안정화, 브랜치 통합
- 참여 규모: 확인된 author 매칭 커밋 1,039건
  - 일반 커밋 566건
  - merge commit 473건

## 담당 역할 요약

BackroomCompany에서는 V206~V208 구간의 기능 개발, 시스템 리워크, 릴리즈 안정화 업무를 담당했다. 초기에는 VOIP/CustomVoice, NamePlate, Modal/ErrorModal, Medal/Leaderboard, Helena/Partygoer, SideMission, StaticRoom, SpecMesh, QA/Debug 중심의 안정화 업무를 수행했고, V208 구간에서는 TechTree, HubStorage, Crafting, Minimap/Repeater, Revival, Inventory/Locker, GameOver Reset, StaticRoom item deploy 등 허브 메타 진행 시스템과 스테이지 런타임 시스템을 확장했다.

---

## 1-1. V206~V207 운영, QA, 기능 개선

### 기간

- 2025.09 ~ 2026.03

### 주요 업무

- VOIP/CustomVoice 시스템 안정화
- PlayerVoice capture, encoder/decoder lifetime, RPC 송수신, jitter buffer, mute/open mic/push-to-talk 흐름 보정
- Modal/ErrorModal, Leaderboard, Medal, NamePlate, IDCard 표시 등 UI/HUD 기능 수정
- Helena/Partygoer creature 상태, StaticRoom creature, SideMissionPool, SpecMesh, GodRay/Light 관련 QA 대응
- SIK/공식 릴리즈 브랜치 병합, 빌드 버전 변경, 패키징/릴리즈 대응
- QA 티켓 기반 크래시, focus, null access, Blueprint enum, DataTable, Widget binding 오류 수정

### 기술 스택

- Unreal Engine C++, Blueprint
- UMG, WidgetComponent, PlayerController, HUD
- RPC, Replication, GameInstanceSubsystem
- Audio capture/playback, CustomVoice/VOIP plugin
- GameplayTag, DataTable, DataAsset
- Git branch/merge workflow

### 상세 기여

- `PlayerVoiceSubsystem`, `PlayerVoiceSpeakComponent`, `PlayerVoiceSynthComponent` 등 음성 시스템 주변에서 초기화 순서, GC 예외, 마이크 장치, proximity/radio playback route 문제를 안정화했다.
- 음성 기능에서 capture loop, VAD, push-to-talk, mute 상태, 서버 경유 RPC, 수신 측 jitter buffer와 playback route를 함께 고려해 멀티플레이 음성 재생 흐름을 보정했다.
- Modal/ErrorModal 및 Leaderboard/Medal UI의 focus, input mode, viewport z-order 문제를 수정하여 메뉴/게임패드/ESC 흐름에서 발생하는 조작 불능 상태를 줄였다.
- NamePlate와 IDCard 이름 표시에서 render target, widget component, replicated display name, client visibility 문제를 다뤘다.
- Helena/Partygoer 관련 creature 상태 전환, 사망/피격/추적/조명/애니메이션 이슈를 QA 티켓 기반으로 수정했다.
- GodRay/Light, flashlight, story stage, static room 관련 콘텐츠/레벨/BP 변경을 C++ 로직과 함께 반영했다.
- `develop_v206`, `develop_v207`, `develop_sik_v206`, `develop_sik_v207`, `main_official_release*` 계열 브랜치 병합을 반복 수행하며 릴리즈 대상 브랜치 안정화에 참여했다.

### 성과/기여 포인트 문장

- 멀티플레이 음성 시스템과 UI focus 문제처럼 재현 조건이 복잡한 QA 이슈를 C++ 런타임, Blueprint, 네트워크 상태를 함께 추적해 해결했다.
- 릴리즈 브랜치 병합과 버전 변경을 병행하며 V206~V207 운영 구간의 안정화와 QA 대응에 기여했다.

---

## 1-2. TechTree 기반 허브 메타 진행 시스템

### 기간

- 2026.03 ~ 2026.05

### 주요 업무

- DataTable + GameplayTag 기반 TechTree core framework 구현 및 보정
- 노드 상태 `Locked / Unlocked / Deactivated / Activated` 관리
- 선행조건, 의존 관계, 최초 활성화 비용, 재활성화, 수동 비활성화, 연쇄 비활성화 로직 구현
- TechTree 진행도 SaveGame 저장/로드 및 성공 변이 후 즉시 저장 훅 구성
- Debug Widget 기반 전체 노드 강제 활성화 및 서버 RPC 경로 구현
- TechTree 상태와 Repeater, Revival, Crafting, Difficulty/GameOver 정책 연동

### 기술 스택

- Unreal Engine C++
- GameplayTag, DataTable, DataAsset
- ActorComponent, GameState, SaveGame
- RPC, Delegate, Replication
- UMG Debug Widget

### 상세 기여

- `UTechTreeManagerComponent` 중심으로 TechTree 노드 정의, 런타임 상태 계산, 활성화/비활성화 API, 비용 처리 인터페이스, executor hook을 구성했다.
- `FGameplayTag NodeTag`를 노드 식별자로 사용하여 C++ 시스템과 DataTable authoring을 분리했다.
- `PrerequisiteNodeTags`와 `DependencyNodeTags`를 분리하여 영구 해금 조건과 현재 활성 유지 조건을 별도 모델로 설계했다.
- `Unlocked`, `Purchased`, `Activated` 상태를 SaveGame에 저장하고 로드 시 런타임 상태로 복원하는 흐름을 구성했다.
- TechTree 진행도가 변경된 직후 `UGameSaveComponent`가 저장하도록 서버 전용 native multicast delegate와 저장 훅을 연결했다.
- Debug UI에서 모든 유효 노드를 강제 활성화하는 `ForceActivateAllNodes()`와 서버 RPC 요청 경로를 구현해 QA 및 밸런스 테스트 효율을 높였다.
- TechTree 노드 상태를 Repeater 수리 상태, Revival 해금, Crafting 접근 조건, GameOver 진행 보존 정책과 연결했다.

### 성과/기여 포인트 문장

- GameplayTag와 DataTable 기반의 확장 가능한 TechTree 코어를 구축하여 허브 메타 진행, 제작, 부활, 리피터 수리 등 여러 시스템이 공통 진행도 상태를 참조할 수 있게 했다.
- 진행도 변경 즉시 저장 구조를 추가해 스테이지 종료 전 비정상 종료나 재접속 시 TechTree 상태가 유실될 가능성을 줄였다.

---

## 1-3. Hub Storage / Crafting / Item Info 시스템

### 기간

- 2026.04 ~ 2026.05

### 주요 업무

- Hub Storage core framework 구현 및 안정화
- authoritative item record와 replicated UI view 분리
- stacked/non-stacked item deposit, withdraw, rollback, consume 처리
- Storage save/load/reset 연동
- Hub Crafting recipe, preview, validation, output queue 구성
- 전역 Item Info DataTable 조회를 `UItemInfoSubsystem`으로 단독화
- Stage end 시 엘리베이터 바닥 드랍 아이템 자동 storage 회수와 inventory-owned item 보존 처리

### 기술 스택

- Unreal Engine C++
- SaveGame, Replication
- DataTable, GameInstanceSubsystem
- UMG/Debug Widget
- Actor spawn, item serialization

### 상세 기여

- `UHubStorageManagerComponent`를 통해 실제 저장 단위인 `FHubStoredItemRecord`와 UI 표시 단위인 `FHubStorageViewEntry`를 분리했다.
- stackable item은 `ItemType + RelicsType` 기반 stack key로 처리하고, non-stackable item은 `EntryId` 기준으로 개별 인출할 수 있게 구성했다.
- deposit 순서인 `DepositOrder`를 유지해 UI 표시, 소비, withdraw 순서가 일관되도록 했다.
- `AHubStorageTerminal`을 storage UI adapter와 output 배출기로 구성하여 withdraw output과 crafted output이 같은 queue를 재사용하도록 정리했다.
- `UHubCraftingManagerComponent`에서 DataTable 기반 recipe 정의, 전역 제작 해금, 개별 recipe 해금, 수량 기반 preview, 재료 보유 상태 payload를 구현했다.
- 제작 성공 시 입력 재료는 storage에서 소비하고 결과물은 terminal 출구에서 물리적으로 배출되도록 provider 구조를 연결했다.
- 전역 아이템 이름, 설명, 가격 범위, 질량 범위 조회를 `UItemInfoSubsystem`으로 단독화해 legacy helper 의존을 제거했다.
- Stage End 과정에서 `OwnerInventory`가 있는 아이템이 room cleanup이나 item cleanup에 의해 삭제되는 문제를 방지하고, 드랍 아이템만 storage 자동 회수 대상으로 유지했다.

### 성과/기여 포인트 문장

- 허브 보관함, 제작, 아이템 정보 조회, 저장/로드를 record 기반으로 통합해 아이템 유실 위험을 줄이고 후속 UI 확장에 필요한 payload 구조를 마련했다.
- authoritative 데이터와 replicated UI view를 분리하여 멀티플레이 환경에서 서버 상태와 클라이언트 표시 상태가 어긋나는 문제를 줄였다.

---

## 1-4. Minimap / Repeater 시스템 리워크

### 기간

- 2026.05

### 주요 업무

- Minimap UI asset ownership 리팩터링
- `UPlayerMinimapController`와 `UWidgetMinimap` 결합 축소
- 플레이어별 large minimap runtime render target 구성
- floor별 player `SpecMesh` icon/nickname 표시 필터링
- Repeater 수리 상태를 TechTree 노드 기준으로 변경
- Repeater 월드 미니맵 구현 및 기존 `WidgetComp` 재사용
- 상호작용자 HUD 미니맵과 비상호작용자 월드 미니맵 표시 상태 분리

### 기술 스택

- Unreal Engine C++
- UMG, WidgetComponent, RenderTarget, SceneCaptureComponent2D
- GameplayTag, TechTree 연동
- RPC/Client local state
- World actor component filtering

### 상세 기여

- minimap/floor material 소유권을 `UPlayerMinimapController`에서 `UWidgetMinimap`으로 이동해 UI asset 세팅 책임을 위젯으로 정리했다.
- minimap 크기 변경, floor 선택/범위 변경, repeater 기반 big minimap 표시 상태를 delegate 기반으로 전달하도록 구조를 개선했다.
- `AMinimapCamera`에 플레이어별 large minimap runtime render target 생성 경로를 추가하고, `UWidgetMinimap`이 `BigMinimapTextureParameterName`으로 render target을 머터리얼에 바인딩하도록 구현했다.
- large minimap에서 현재 선택된 floor와 다른 floor의 player `SpecMesh` icon/nickname을 숨기도록 floor 필터링을 보정했다.
- 리피터의 기존 welder/chipset/monkeyspanner 기반 수리 상태를 제거하고 `TechTree.Tier04.03.FixRepeater` 노드 활성 여부로 수리 여부를 판단하도록 정리했다.
- `ARepeater`의 기존 `WidgetComp`를 월드 미니맵 전용 표면으로 재사용하고, 수리 완료 상태에서는 interaction open/close와 무관하게 월드 미니맵 표면이 유지되도록 구현했다.
- 상호작용자는 기존 HUD 큰 미니맵을 사용하고, 다른 플레이어는 HUD/input mode 변경 없이 리피터 월드 화면만 볼 수 있도록 local render target 바인딩 경로를 분리했다.

### 성과/기여 포인트 문장

- 개인 HUD 미니맵과 월드에 공유되는 리피터 미니맵을 같은 위젯/렌더 타겟 구조를 재사용하면서도 로컬 플레이어별 상태가 충돌하지 않도록 분리했다.
- 수리 상태를 TechTree 기반으로 일원화하여 리피터 기능이 허브 진행도와 자연스럽게 연결되도록 리워크했다.

---

## 1-5. Revival / Locker / Inventory / Stage End 안정화

### 기간

- 2026.05

### 주요 업무

- TechTree 기반 자동 부활/IDCard 부활 시스템 개편
- `UStageRevivalManagerComponent` 추가 및 서버 권한 부활 큐 관리
- IDCard 판매, 부활 예약, 환불, Quota/재화 구조 연동
- LockerKeyRing 탈출 크래시 수정 및 Locker registry 생명주기 정리
- StaticRoom invalid room locker availability 처리
- Stage End inventory-owned item 삭제 방지

### 기술 스택

- Unreal Engine C++
- ActorComponent, GameState, PlayerStatus, Revival flow
- RPC, 서버 권한 상태 관리
- SaveGame, Inventory, Item ownership
- StaticRoom generation, LockerManager

### 상세 기여

- 기존 IDCard 판매 기반 단일 부활 흐름을 TechTree 기반 자동 부활과 IDCard 부활로 분리했다.
- `UStageRevivalManagerComponent`를 추가하여 자동 부활 사용 여부, IDCard 부활 예약/완료 상태, 부활 요청 큐, 전원 사망 지연 조건을 서버 권한으로 통합 관리했다.
- 자동 부활 노드가 활성화된 경우 첫 사망 유저를 IDCard 드랍 없이 부활시키고, 자동 부활 진행 중 전원 사망 이벤트가 발생해도 스테이지 종료를 지연하도록 보정했다.
- IDCard 부활 노드가 비활성인 경우 IDCard 판매가 기존처럼 재화 지급으로 처리되도록 분기했고, 부활 예약에 성공한 IDCard는 판매 금액 계산에서 제외했다.
- 부활 실패 시 이미 판매 정산에서 제외된 금액을 환불할 수 있도록 예약 당시 금액 기록과 복구 경로를 구성했다.
- `LockerManager`에서 빈 locker 배열 인덱싱으로 발생하는 `LockerKeyRing` 탈출 크래시를 제거하고, `ALocker`가 BeginPlay/EndPlay에서 자동 등록/해제되도록 정리했다.
- StaticRoom graph cleanup 이후 invalid room의 locker를 파괴하지 않고 gameplay unavailable 상태로 전환해 random exit 후보에서 제외했다.
- Stage End cleanup에서 `OwnerInventory`가 있는 item actor가 삭제되지 않도록 room deployed tracking과 item cleanup guard를 보강했다.

### 성과/기여 포인트 문장

- 사망, 부활, 판매, 재화, 스테이지 종료가 얽힌 복잡한 멀티플레이 상태를 서버 권한 컴포넌트로 정리해 부활 시스템의 중복 처리와 진행 중 스테이지 종료 문제를 줄였다.
- Inventory ownership과 room cleanup 경계를 명확히 하여 스테이지 종료 후 플레이어 인벤토리 아이템이 유실되는 문제를 해결했다.

---

## 1-6. StaticRoom / Item Deploy / GameOver Reset / QA 안정화

### 기간

- 2026.04 ~ 2026.05

### 주요 업무

- StaticRoom item minimum depth rule 보정
- item spawn replacement fallback 및 minimum count 보호
- invalid room locker candidate 제외
- 난이도별 GameOver reset 정책 구현
- Easy/Normal/Hardcore별 보존 데이터 분리
- FlashLight, Repeater Minimap, Item Deploy, TechTree UI/Popup, Storage, Stage Result, Revival Widget 등 릴리즈 전 디버깅

### 기술 스택

- Unreal Engine C++
- StaticRoom generation, item spawn/deploy
- SaveGame, runtime reset
- GameplayTag, TechTree
- UMG/HUD QA

### 상세 기여

- `UStaticRoomItemDeployer::SpawnMinimumItems()`가 depth별 item weight rule을 우회하지 않도록 검증 로직을 추가했다.
- minimum 부족분 보정 중 빈 spawn point가 없을 때, 같은 depth rule에서 target item이 허용되는 occupied point를 안전하게 교체하는 replacement fallback을 추가했다.
- replacement victim은 해당 item의 minimum count를 침범하지 않도록 보호했다.
- 난이도별 GameOver reset 정책을 구현해 Easy는 재화/TechTree/Storage를 보존, Normal은 TechTree만 보존, Hard는 `FixPowerLeak` 노드만 예외 보존하도록 SaveGame snapshot 정책을 분리했다.
- 게임오버 후 같은 세션에서 runtime manager 상태를 저장 snapshot과 다시 맞추고, `bIsGameOvered` 플래그가 일반 저장에 남아 재초기화 루프를 만드는 문제를 방지했다.
- V208 릴리즈 직전 FlashLight attach/view state, item deploy, locker availability, stage result, revival widget, TechTree popup, storage LQA text 등 다양한 QA 이슈를 수정했다.

### 성과/기여 포인트 문장

- 스폰 규칙, 난이도별 저장 정책, Stage End cleanup 등 플레이 세션의 안정성과 직결되는 시스템을 보정해 릴리즈 전 회귀 위험을 줄였다.
- QA 재현 조건을 코드, 데이터, Blueprint, 월드 배치까지 추적하며 멀티플레이 런타임 안정화에 기여했다.

---

# 프로젝트 2. BackroomCleaners

## 프로젝트 개요

- 프로젝트: BackroomCleaners
- 장르/성격: Unreal Engine 기반 액션/협동 게임
- 업무 기간: 2025.11 ~ 2026.04  
  - Git 이력 확인 범위: 2025.11.21 ~ 2026.04.09
- 담당 역할: Unreal Engine C++ gameplay/UI/rendering/audio 개발
- 참여 규모: 확인된 커밋 93건
  - 실 변경 커밋 64건
  - merge commit 29건

## 담당 역할 요약

BackroomCleaners에서는 엔티티 렌더링 최적화, 전투 BGM 트리거, Hero 카메라/이동 조작감, 공격 피드백 VFX, Crosshair/HUD, EntityHealthBar/EntityStatusBar 시스템 구현을 담당했다. 특히 `CreatureVertexAnimator` 기반 VAT 적용, EntityHealthBar tracker/widget/module 구조, BGMSubsystem 전환, player-local UI 표시 구조 등 C++ 시스템과 에셋/BP 연동이 필요한 작업을 수행했다.

---

## 2-1. VAT 기반 엔티티 렌더링 최적화

### 기간

- 2025.11 ~ 2025.12

### 주요 업무

- Creature/Entity에 Vertex Animation Texture 기반 렌더링 적용
- `CreatureVertexAnimator` MID 설정 및 Entity 연결
- Parasyte/Turret VAT 에셋, material, texture, static mesh 구성
- LOD 기반 VAT 분리 및 Instanced Component 전환
- Dynamic Shadow, Outline Interval, Invisible Entity, Double Mesh, Divide Zero, Loop Animation 등 표시 문제 수정

### 기술 스택

- Unreal Engine C++
- Material Instance Dynamic, VAT, LOD
- Instanced Component
- Blueprint/StaticMesh/Material/Texture asset
- Render/visibility debugging

### 상세 기여

- `CreatureVertexAnimator`를 중심으로 VAT 컴포넌트를 구성하고 EntityBase, EntityAnimInstance와 연결했다.
- Parasyte/Turret 엔티티에 VAT data asset, material instance, texture, static mesh를 적용해 기존 skeletal animation 기반 표시 비용을 줄이는 방향의 렌더링 최적화 작업을 수행했다.
- LOD 기준으로 VAT 적용 경로를 분리하고 HyperMassLOD 설정과 연동했다.
- VAT 적용 후 발생한 invisible entity, double mesh, divide zero, loop animation, dynamic shadow 문제를 순차적으로 수정했다.
- 불필요한 코드와 런타임 조건을 정리해 EntityBase와 CreatureVertexAnimator 사이의 책임을 명확히 했다.

### 성과/기여 포인트 문장

- VAT 기반 엔티티 표시 구조를 적용하고 LOD/Material/Blueprint 에셋을 함께 정리해 다수 엔티티 렌더링 최적화 기반을 마련했다.
- 최적화 적용 이후 발생한 표시 오류를 렌더링, 애니메이션, material, entity runtime 조건을 함께 추적해 안정화했다.

---

## 2-2. Battle BGM / Hero 카메라·이동 / 공격 피드백

### 기간

- 2025.11 ~ 2025.12

### 주요 업무

- 로비/오페라 레벨 BGM trigger 배치 및 전환 구현
- `BCSTask_ControlBGM` 기반 BGM 제어를 `BGMSubsystem`으로 이전
- TempEventBindingTrigger multicast 처리 추가
- Hero crouch camera offset, landing camera shake, air duck, capsule height, mesh pop 수정
- 근접 공격 swing trail Niagara 연동
- melee/projectile kill sound client-side prediction 처리

### 기술 스택

- Unreal Engine C++
- GameInstanceSubsystem 또는 runtime subsystem
- Trigger/Task, NetMulticast
- CameraShake, SpringArmComponent
- Niagara, Sound, Action system

### 상세 기여

- 로비와 오페라 레벨에 BGM trigger를 배치하고, 시퀀스/트리거 기반으로 BGM을 제어하는 흐름을 구성했다.
- 초기 `BCSTask_ControlBGM` 중심의 제어를 `BGMSubsystem`으로 이전하여 BGM 상태 관리의 의존도를 낮추고 재사용성을 높였다.
- `TempEventBindingTrigger`에 multicast 처리를 추가해 네트워크 환경에서도 트리거 전파가 가능하도록 보강했다.
- `BCSSpringArmComponent`에 crouch camera offset 조정 기능을 추가하고, Marvin landing camera shake를 적용했다.
- jump crouch를 air duck 흐름으로 교정하고 capsule height와 mesh pop 현상을 수정해 조작감과 시각 피드백을 개선했다.
- ActionBase, ActionManager, ActionMeleeAttack, EquipmentMelee 경로를 통해 근접 공격 trail 발생 흐름을 액션 기반으로 정리했다.
- melee/projectile kill sound에서 client-side prediction 재생 흐름을 보강했다.

### 성과/기여 포인트 문장

- 오디오 트리거, 카메라 피드백, 이동 상태, 공격 VFX를 gameplay action 흐름에 맞춰 연결해 전투 조작감과 피드백 품질을 개선했다.
- BGM 제어를 subsystem 구조로 옮겨 레벨/트리거 의존도를 낮추고 네트워크 트리거 전파를 보강했다.

---

## 2-3. Crosshair / HUD 피드백

### 기간

- 2025.12

### 주요 업무

- 무기별 Adaptive Crosshair 구현
- `EquipmentStruct`에 Crosshair 관련 데이터 확장
- `WidgetCrossHair`가 장비 상태에 맞춰 HUD 표시를 변경하도록 수정
- Axe, ChinaLake, L1A1, M16A1 등 무기 Blueprint 설정 반영

### 기술 스택

- Unreal Engine C++
- UMG HUD Widget
- Equipment data struct
- Weapon Blueprint

### 상세 기여

- 장비 데이터 구조에 Crosshair 표시 정보를 추가하고, HUD 위젯이 현재 장비 상태를 기반으로 표시를 바꾸도록 구성했다.
- 무기별 Blueprint에 Crosshair 설정을 반영해 장비 교체 시 시각 피드백이 자연스럽게 바뀌도록 했다.

### 성과/기여 포인트 문장

- 무기별 Crosshair 피드백을 데이터 기반으로 확장해 HUD 표시와 장비 상태의 일관성을 높였다.

---

## 2-4. EntityHealthBar / EntityStatusBar 시스템

### 기간

- 2026.03 ~ 2026.04

### 주요 업무

- EntityHealthBar tracker framework 구현
- local-only 체력바 표시 구조 설계
- `PlayerController / EntityBase -> TrackerActor -> WidgetComponent -> RootWidget -> HP/Buff/Identity Module` 흐름 구현
- 중앙 조준 reveal, 로컬 플레이어 피해 reveal 구현
- tracker actor pool, Screen Space Widget, 거리 기반 scale, 위험도별 widget class 분기 구현
- HP/Buff/Identity widget module 구조와 BP event fan-out 구성
- EntityHealthBar 문서 작성 및 최신 구조 정리

### 기술 스택

- Unreal Engine C++
- UMG, Screen Space Widget, WidgetComponent
- ActorComponent, Interface, Object Manager
- Local-only UI, line trace, distance-based scale
- Data provider/event receiver pattern

### 상세 기여

- `UEntityHealthBarTrackerObjectManager`, `AEntityHealthBarTrackerActor`, `UEntityHealthBarWidgetComponent`, `UWidgetEntityHealthBarBase` 계열 클래스를 추가해 엔티티 체력바 추적 프레임워크를 구축했다.
- `AMainPlayPlayerController::UpdateAim()`의 중앙 trace와 `AEntityBase::OnTakeDamage()`의 로컬 플레이어 피해 이벤트를 통해 체력바 reveal 경로를 구성했다.
- `AEntityBase`가 `IEntityHealthBarDataProvider`와 `IEntityHealthBarEventReceiver` 역할을 수행하도록 연결해 표시 가능 여부와 데이터 공급을 분리했다.
- tracker actor가 target actor와 offset을 추적하고 hold, fade-out, death fade 이후 pool로 반환되도록 구현했다.
- `UEntityHealthBarWidgetComponent`에서 위험도별 widget class 선택, line trace 기반 표시 가능성 판정, 거리 기반 scale 보정을 처리했다.
- root widget이 HP/Buff/Identity module에 actor, alpha, scale, death 상태를 fan-out하도록 모듈형 구조를 구성했다.
- HP stat binding, buff/debuff polarity 기반 이벤트, identity/name surface를 BP 위젯에서 소비할 수 있도록 구성했다.
- legacy HP module을 제거하고 `UWidgetHealthBarModule` 중심으로 경로를 정리했으며, tracker manager 획득 경로를 특정 GameState getter 의존에서 generic component 검색 구조로 개선했다.

### 성과/기여 포인트 문장

- 전투 중 필요한 엔티티 정보만 local-only로 표시하는 EntityHealthBar 시스템을 설계하고, tracker actor와 widget module 구조로 확장 가능한 UI 프레임워크를 구현했다.
- 조준, 피격, 위험도, 거리, 가시성, 사망 fade를 하나의 표시 흐름으로 통합해 전투 HUD의 정보 전달력을 높였다.

---

# 경력기술서용 요약 문장

## 한 문단 요약

Unreal Engine 기반 멀티플레이 게임 프로젝트에서 C++ gameplay system, UI/HUD, 네트워크 동기화, 저장/로드, 데이터 기반 진행 시스템, 렌더링 최적화, QA 안정화 업무를 수행했다. `BackroomCompany`에서는 V206~V208 구간의 TechTree, HubStorage, Crafting, Minimap/Repeater, Revival, Inventory/Locker, StaticRoom, VOIP/CustomVoice, Modal/Leaderboard/NamePlate 등 주요 시스템 개발 및 릴리즈 안정화를 담당했으며, `BackroomCleaners`에서는 VAT 기반 엔티티 렌더링 최적화, EntityHealthBar/EntityStatusBar, BGMSubsystem, Hero 카메라/이동, Crosshair/HUD, 공격 VFX 시스템을 구현했다.

## 이력서 상단 핵심 bullet

- Unreal Engine C++ 기반 멀티플레이 gameplay system 및 UI/HUD 개발 경험
- GameplayTag/DataTable/SaveGame 기반 TechTree, Crafting, Storage 등 허브 메타 진행 시스템 구현
- RPC, Replication, 서버 권한 컴포넌트 기반 Revival, VOIP, Repeater, Minimap 등 네트워크 기능 안정화
- UMG, WidgetComponent, RenderTarget, Screen Space Widget 기반 HUD/Modal/Minimap/HealthBar 구현
- VAT, LOD, Material Instance, Niagara, Sound, CameraShake 등 렌더링/피드백 시스템 구현
- QA/Debug 중심의 크래시, null access, focus, item loss, save/load, stage transition, 패키징 오류 대응

## 면접에서 강조할 만한 기여

- TechTree를 단순 UI 기능이 아니라 허브 메타 진행의 공통 상태 시스템으로 설계하고, Repeater, Revival, Crafting, GameOver 정책이 같은 진행도를 참조하도록 연결했다.
- Storage/Crafting에서는 서버 authoritative record와 클라이언트 UI view를 분리해 멀티플레이 환경의 데이터 일관성을 확보했다.
- Repeater World Minimap에서는 같은 리피터 화면을 보더라도 각 클라이언트가 자기 local render target을 바인딩하도록 구현해 네트워크 텍스처 복제 없이 공유 월드 화면을 구성했다.
- EntityHealthBar에서는 local-only tracker actor와 screen-space widget module 구조를 도입해 전투 정보 표시를 확장 가능한 구조로 만들었다.
- QA 안정화 업무에서는 코드, Blueprint, DataAsset, World Partition external actor가 함께 엮인 Unreal 프로젝트 특성을 고려해 원인 추적과 회귀 테스트를 수행했다.

---

# 보완이 필요한 정보

아래 정보가 추가되면 실제 제출용 경력기술서 품질이 더 좋아진다.

1. 회사명, 재직 기간, 직급/직책의 정확한 표기
2. 각 프로젝트의 출시 여부, 플랫폼, 팀 규모
3. 본인이 맡은 시스템 중 실제 라이브/릴리즈에 반영된 항목
4. 정량 성과
   - 크래시 감소, QA 티켓 처리 수, 성능 개선 수치, 빌드/패키징 안정화 횟수 등
5. 지원 포지션
   - 클라이언트 프로그래머, 언리얼 게임플레이 프로그래머, UI 프로그래머, 시스템 프로그래머 중 어느 방향인지
6. 공개 가능한 프로젝트 설명 범위
   - 내부 프로젝트명 그대로 사용 가능한지, 일부 기능명을 일반화해야 하는지
